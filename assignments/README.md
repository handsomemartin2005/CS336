# Assignments

This folder tracks CS336 assignment work. Keep each assignment self-contained:

- implementation notes in `notes.md`
- command snippets in `commands.md`
- experiment results in `experiments.md`
- final answers or writeups in `writeup.md`

Use `scripts/import-assignment.ps1` to copy official starter code into a matching
assignment folder when you are ready to start.
